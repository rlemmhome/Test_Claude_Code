# Test_Claude_Code

A minimal Java project for testing Claude Code.

## Requirements

- JDK 21+

## Build & Run

```bash
javac --enable-preview --release 21 -d out src/Main.java
java --enable-preview -cp out Main
```